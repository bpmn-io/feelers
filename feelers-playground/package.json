{
  "private": true,
  "name": "feelers-playground",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "cross-env preact build --no-prerender",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "cross-env preact watch --no-prerender --no-refresh",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "cross-env": "^7.0.3",
    "preact-cli": "^4.0.0-next.6",
    "sirv-cli": "^2.0.0"
  },
  "dependencies": {
    "preact": "^10.18.1",
    "preact-markup": "^2.1.1",
    "showdown": "^2.1.0"
  }
}
